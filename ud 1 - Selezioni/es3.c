#include <stdio.h>
int main () {
    int n1;
    int n2;
    int n3;
    int terna;

    printf("Inserisci il primo valore:\n");
    scanf("%d", &n1);

    printf("Inserisci il secondo valore:\n");
    scanf("%d", &n2);

    printf("Inserisci il terzo valore:\n");
    scanf("%d", &n3);

    if (n1 >= n2 && n1 >= n3 && n2 >= n3) {

        printf("Sono in ordine crescente\n")

        int terna == n1^2*n2^2
    }    

    else {
        printf("I numeri non sono in ordine cresce per ciò nessuna terna pitagorica")
    }

    if (terna=n3^2){
        printf("è una terna pitagorica")
    }

    else {
        printf("non è une terna")
    }

    return 0;

}